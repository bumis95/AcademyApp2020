<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <!-- create gradient you want to use with the angle you want to use -->
        <shape android:shape="rectangle">
            <gradient
                android:angle="90"
                android:startColor="@color/gradient_radial_stroke_startcolor"
                android:endColor="@color/gradient_radial_stroke_endcolor" />
            <corners android:radius="8dp"/>
        </shape>
    </item>
    <!-- create the stroke for top, left, bottom and right with the dp you want -->
    <item
        android:bottom="1dp"
        android:left="1dp"
        android:right="1dp"
        android:top="1dp">
        <shape android:shape="rectangle">
            <corners android:radius="8dp"/>
            <!-- fill the inside in the color you want (could also be a gradient again if you want to, just change solid to gradient and enter angle, start, maybe center, and end color) -->
            <gradient
                android:type="radial"
                android:gradientRadius="@dimen/indent_24"
                android:angle="90"
                android:startColor="@color/gradient_radial_startcolor"
                android:endColor="@color/gradient_radial_endcolor" />
        </shape>
    </item>
</layer-list>
